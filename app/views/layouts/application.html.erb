<!DOCTYPE html>
<html lang="en">

	<%= render 'layouts/head' %>

	<%= render 'layouts/header' %>
	<body>
		<main>
			<%= yield %>
		</main>

		<%= render 'layouts/footer' %>

		<%= render 'layouts/top_button' %>

	</body>
</html>
